// OWNER: the style-panel strand (M4). Replace the export; do not touch main.ts.
import type { StylePanel } from '../../app/contracts';
import { draftingStylePanel } from './style-panel';
export const stylePanel: StylePanel = draftingStylePanel;
