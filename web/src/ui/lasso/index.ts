// OWNER: the lasso strand (M4). Replace the export; do not touch main.ts.
import { stubLasso } from '../../app/stubs';
import type { LassoController } from '../../app/contracts';
export const lassoController: LassoController = stubLasso;
