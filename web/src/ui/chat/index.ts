// OWNER: the chat strand (M4). Replace the export; do not touch main.ts.
import { stubChat } from '../../app/stubs';
import type { ChatPanel } from '../../app/contracts';
export const chatPanel: ChatPanel = stubChat;
