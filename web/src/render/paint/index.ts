// OWNER: the paint strand. Replace the export; do not touch main.ts.
import { stubPaint } from '../../app/stubs';
import type { PaintEngine } from '../../app/contracts';
export const paintEngine: PaintEngine = stubPaint;
